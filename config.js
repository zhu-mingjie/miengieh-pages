// TODO DOWNLOAD_URLS: Replace these empty strings with official HTTPS links.
// Non-empty valid HTTPS URLs turn the corresponding buttons into normal links.
window.MIENGIEH_DOWNLOAD_URLS = Object.freeze({
  chromeWebStore: "",
  github: ""
});
